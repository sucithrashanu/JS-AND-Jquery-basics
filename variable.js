

     
        //JS is loosely typed. no need to tell what type of data it is.
        //jS is dynamically typed type of data will change dynamically
        
        var num = 16; // number
        var Name = "Trupti PArsai"; //String
        var flag = false; //boolean
        
        document.write(num);
        alert(Name);

        //jS is dynamically typed type of data will change dynamically
        var num = "Arpit"; //now num is string
        document.write(num);
        /* Naming convention for var called identifier 
        can't use keyword
        first letter can be _, $
        vriables are case sensitive.
        */
       


     